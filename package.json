{
  "name": "code",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "serve",
    "build": "esbuild header-form-logic.js --bundle --outfile=dist/header-form-logic.js --format=esm --watch --sourcemap"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.25.8",
    "serve": "^14.2.4"
  }
}
